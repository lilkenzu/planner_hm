# Состояние проекта

> Обновляется в конце каждой сессии. Описывает «как есть сейчас», а не историю
> изменений — старое перезаписывается. Первым делом в новой сессии агент читает
> именно этот файл.

**Обновлено:** `2026-08-16, сессия 1` (после шага 2 плана)

## Готово

- Тема выбрана: «Планировщик встреч» (Project/Product, окно, удобное всем).
- `SPEC.md` заполнен полностью: пользователь, сценарии, состояния, нецели,
  допущения (включая округление процента и границы рабочего часа), открытые
  вопросы (полный Q&A), критерии приёмки, план реализации на 8 шагов.
- Часть II `AGENTS.md` дописана и синхронизирована со SPEC.md (стек, команды,
  запреты, когда спрашивать, что проверять перед «готово», соглашения).
- Наивный прогон сделан пользователем самостоятельно, вне этой сессии/чата —
  ссылка на папку будет добавлена позже, после завершения основной работы.
- **Шаг 1 плана — «Форма и список участников» — реализован и проверен вживую.**
  Файлы: `index.html`, `app.js`, `styles.css`. Добавление/редактирование/удаление
  участника через форму, ошибка нераспознанного города (без падения формы).
  Проверено в браузере через `python3 -m http.server 8765`, консоль чистая.
  Логики пересечений и сетки ещё нет — это следующие шаги.
- `.claude/launch.json` — конфигурация для предпросмотра в браузере.
- Git-репозиторий инициализирован, привязан к `github.com/lilkenzu/planner_hm`
  по SSH (см. `TOOLS.md`) и запушен (ветка `main`).
- **Шаг 2 плана — «Локальное время участника через `Intl`/`Date`» — реализован
  и проверен.** Файл `availability.js` (без DOM-зависимостей, чтобы работать и
  в браузере, и в `node`): `getZonedDateParts(date, timeZone)` конвертирует
  произвольный UTC-момент в локальные год/месяц/день/час/минуту/день недели.
  `tests/availability.test.js` — 3 автотеста (Катманду с переходом через
  полночь, Катманду без перехода, Москва), все зелёные, с перекрёстной сверкой
  через независимый вызов `Intl`. Дополнительно проверено вручную в браузере —
  результат совпал с `node`.

## В работе

- Ничего не в процессе — шаг 2 закрыт и закоммичен, ждём подтверждения перехода
  к шагу 3.

## Не начато

- Шаги 3–8 плана реализации: сетка на 7 дней и расчёт доступности участников
  (дальнейшее расширение `tests/availability.test.js`), отображение сетки и
  детали слота, состояние «нет полного совпадения», пустое состояние/ошибки,
  проверка пограничных случаев (Москва/Лондон DST, Катманду +5:45), финальная
  проверка.
- Независимый аудит (другая модель / чистая сессия).
- `README.md` с итоговым отчётом и сравнением наивного и спека-прогона.
- Ссылка на папку с наивным прогоном пользователя (сделан вне этой сессии) —
  добавить в README, когда пользователь её даст.

## Активные инструменты

Вся логика продукта — на встроенных браузерных `Intl`/`Date`, чистый JS без
библиотек и без сборки (см. `AGENTS.md` → Часть II → Стек и версии). Локальный
сервер для предпросмотра — `python3 -m http.server 8765`. Автотест —
`node tests/availability.test.js`, без внешних тестовых библиотек. Git —
SSH-ключ без пароля, подробности в `TOOLS.md` за 2026-08-16.

## Известные проблемы

- Валидация «начало рабочего дня раньше окончания» добавлена в шаге 1 по
  собственному решению агента (не обсуждалась явно в SPEC.md/вопросах) —
  необходима для корректности будущего расчёта доступности, но формально это
  самостоятельное решение, а не пункт спеки. Зафиксировано в `session-1.md`.
- SSH-ключ для push сгенерирован без пароля — осознанный компромисс ради
  неинтерактивного push агентом, но снижает защиту ключа при краже файла.
  Приемлемо для учебного репозитория, но не для боевого проекта. См. `TOOLS.md`.

## Следующий шаг

По подтверждению пользователя — шаг 3 плана: сетка на 7 календарных дней с
шагом 30 минут и расчёт доступности участников (округление до целого процента,
границы рабочего часа, автотест на составе участников из разных поясов/недель,
включая получасовой сдвиг и переход через полночь). Постановка и сверка перед
кодом — как на шагах 1–2.
